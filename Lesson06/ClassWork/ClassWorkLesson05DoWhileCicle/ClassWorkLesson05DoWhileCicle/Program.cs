using System;

namespace ClassWorkLesson05DoWhileCicle
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Введите не больше 15 символов");
            string input = Console.ReadLine();
            //int inputParsedInt = int.Parse(input);
            do
            {
                if (input.Length > 15)
                    Console.WriteLine ($"Вы ввели ")
                    break;
                if ()
                    continue;
            }
            while (true);
        }
    }
}
//глянуть видос